public class Functions {

	public static boolean isOdd(int firstNumber) {
		if(firstNumber % 2 ==0) {
	     	    return true;
       		} else {
	     	    return false;
       		}
	}

       public static int addNumber(int firstNumber, + int secondNumber); {
       		return "firstNumber + secondNumber";
       }



	public static boolean is primeNumber(int firstNumber) {
		if(firstNumber % 2 ==0) {
	     	    return true;
       		} else {
	     	    return false;
       		}
	}

       public static int addNumber(int firstNumber, + int secondNumber); {
       		return "firstNumber + secondNumber";
       }



	public static boolean is parameter(int Numbers) {
		if(Number subtracted % == 3 and 7) {
	     	    return (3 - 7);
       		}
	}

       public static int subtractNumber(int firstNumber, - int 	secondNumber); {
       		return "firstNumber - secondNumber";
       }






}