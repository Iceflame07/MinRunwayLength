import java.util.Scanner;
   public class CheckPassword {
   public static void main(String[] args) {

	Scanner input = new Scanner(System.in);

	System.out.println("Enter password: ");
	int password = input.nextInt();

	System.out.println("Enter password with at least ten values: ");
	int values = input.nextInt();

	System.out.println("Enter password consist of only values and integers: ");
	int integers = input.nextInt();

	System.out.println("Enter password must have at least three integers: ");
	int threeintegers = input.nextInt();


	int  and integers = password;
	int three integers = invalid;

	  if(Password + length * 10){
	  	System.out.print(password must have 10 integer)
	    }
	  if(Password + values + integer){
	        System.out.print(Password with only values and integers)
	    }
	  else (Password + length + values + integer){
		System.out.print(length of Password)
	    }

   }

}
	